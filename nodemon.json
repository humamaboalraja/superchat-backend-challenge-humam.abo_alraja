{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["node_modules", "coverage", "dist"],
  "exec": "ts-node -r tsconfig-paths/register ./graphql/server.ts",
  "restartable": "rs",
  "env": {
    "NODE_ENV": "development"
  }
}